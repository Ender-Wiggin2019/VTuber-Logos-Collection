import { IBrand } from "../type";
<% data.forEach(function(d) { %>import { <%= d.brand %> } from "./<%= d.path %>";
<% }); %>

export const <%= name %>: IBrand[] = [
<% data.forEach(function(d) { %><%= d.brand %>,
    <% }); %>
]